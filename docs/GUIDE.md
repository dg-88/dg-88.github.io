# dg-88.github.io — プライバシーポリシー管理ガイド（権威ある単一ソース / docs/GUIDE.md）

> このファイル `docs/GUIDE.md` がこのリポジトリの **権威ある単一ソース**。ルート `CLAUDE.md`（Claude 用・`@import` で自動ロード）と `AGENTS.md`（Codex 用）は本ファイルを指す薄いポインタで、ルールを持たない（drift 防止）。ルールの変更は必ず本ファイルに行う。

このリポジトリは、私が公開している **iPhoneアプリ各種のプライバシーポリシー** をまとめて掲載するサイトです。
GitHub Pages（`dg-88.github.io`）を通じて一般公開され、App Store Connect の「プライバシーポリシーURL」に使用されます。

---

## 📁 フォルダ構成

各アプリごとに1フォルダを作成し、以下のように配置します：
